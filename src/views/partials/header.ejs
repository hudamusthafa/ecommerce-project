<nav class="navbar">

  <!-- LEFT -->
  <div class="logo">
    Aura
  </div>

  <!-- CENTER -->
  <div class="nav-links">

    <a href="/home">Home</a>

    <a href="#">Shop</a>

    <a href="/products">Products</a>

  </div>

<!-- TOAST NOTIFICATION -->
<div id="toast" class="toast hidden">
  <span id="toast-msg"></span>
</div>

<style>
.toast {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%); 
  background: #2e7d32;
  color: white;
  padding: 16px 32px;
  border-radius: 8px;
  font-size: 16px;
  z-index: 9999;
  box-shadow: 0 4px 20px rgba(0,0,0,0.3);
  transition: opacity 0.4s ease;
}

.toast.success {
  background: #2e7d32;
}
.toast.warning {
  background: #e65100; 
}
.toast.hidden {
  opacity: 0;
  pointer-events: none;
}
</style>

<script>
function showToast(message, type = "success") {
  const toast = document.getElementById("toast");
  const toastMsg = document.getElementById("toast-msg");

  toastMsg.innerText = message;
  toast.className = "toast " + type;

  // Auto hide after 3 seconds
  setTimeout(() => {
    toast.className = "toast hidden";
  }, 3000);
}

//  Auto trigger on page load if added or wishlisted
window.addEventListener("DOMContentLoaded", function () {
  const params = new URLSearchParams(window.location.search);

  if (params.get("added")) {
    showToast("✓ Added to cart!");
  }

  if (params.get("wishlisted")) {
    showToast("✓ Added to wishlist!");
  }
  if(params.get("alreadyCart")){
    showToast("Already in cart!","warning")
  }
   if (params.get("alreadyWishlist")) {
    showToast("⚠ Already in wishlist!", "warning");
  }
  // Cart errors
if(params.get("error")){
  showToast("⚠ " + params.get("error"), "warning");
}
});
</script>




  <!-- RIGHT -->
<div class="nav-right">

  <% if(user){ %>

    <span class="welcome">
      Welcome <%= user.name %>
    </span>

    <!-- ACCOUNT DROPDOWN -->
    <div class="account-dropdown">

     <button class="account-btn">
        <i class="fa-solid fa-user"></i>
      </button>

      <div class="account-menu">
        <a href="/profile">Profile</a>
        <a href="/orders">Orders</a>
      </div>

    </div>

    <!-- WISHLIST -->
    <a
      href="/wishlist"
      class="wishlist-icon"
      title="Wishlist"
    >
      <i class="fa-regular fa-heart"></i>
    </a>

    <!-- CART -->
    <a
      href="/cart"
      class="cart-icon"
      title="Cart"
    >
      <i class="fa-solid fa-cart-shopping"></i>

      <% if(cartCount > 0){ %>
        <span class="cart-badge">
          <%= cartCount %>
        </span>
      <% } %>

    </a>

    <!-- LOGOUT -->
    <a href="/logout" class="logout">
      Log Out
    </a>

  <% } else { %>

    <a href="/login" class="login-btn">
      Login
    </a>

    <a href="/register" class="signup-btn">
      Sign Up
    </a>

  <% } %>

</div>
</nav>

